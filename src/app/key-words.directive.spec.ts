import { KeyWordsDirective } from './key-words.directive';

describe('KeyWordsDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyWordsDirective();
    expect(directive).toBeTruthy();
  });
});
